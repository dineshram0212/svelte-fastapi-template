{
  "name": "fastapi-full-stack-template",
  "private": true,
  "workspaces": [
    "admin",
    "frontend"
  ],
  "scripts": {
    "dev": "pnpm -r dev",
    "dev:app": "pnpm --filter frontend dev",
    "dev:admin": "pnpm --filter admin dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
